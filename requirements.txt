# Compatibility entry point for source-repository workflows pinned to older
# Testing commits. Product dependencies must be installed from the exact App or
# Server checkout first; this file deliberately contains no product stack.
-r requirements-desktop.txt
-r requirements-server.txt
